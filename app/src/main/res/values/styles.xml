<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="image_button_tint">@color/bluish_green</item>
        <item name="android:colorBackground">@color/colorBackground</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.DayNight"/>

    <style name="ListItemPrimaryTextAppearance" parent="android:style/TextAppearance.Material.Large">
        <item name="android:textSize">16sp</item>
    </style>

    <style name="ListItemSecondaryTextAppearance" parent="android:style/TextAppearance.Material.Medium">
        <item name="android:textSize">14sp</item>
    </style>

    <style name="ListItemMetadataTextAppearance" parent="android:style/TextAppearance.Material.Medium">
        <item name="android:textSize">12sp</item>
    </style>
</resources>
